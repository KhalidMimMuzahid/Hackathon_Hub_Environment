# Development Environment Variables for Local Development
# This file is configured for local development with PostgreSQL

# Database Configuration
POSTGRES_DB=hub
POSTGRES_USER=postgres
POSTGRES_PASSWORD=devpass123

# API Configuration
API_SECRET_KEY=dev_secret_key_for_local_development_only
DATABASE_URL=postgresql+asyncpg://postgres:devpass123@db:5432/hub

# Server Configuration (localhost for development)
SERVER_IP=localhost
DOMAIN_NAME=localhost

# CORS Origins (localhost for development)
CORS_ORIGINS=http://localhost:3001,http://localhost:3000,http://localhost:8000,http://127.0.0.1:3001,http://127.0.0.1:3000,http://127.0.0.1:8000

# Next.js Configuration (localhost for development)
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# Environment
NODE_ENV=development
ENVIRONMENT=development
DEBUG=true

# Optional: Email Configuration (if you plan to add email features)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# Optional: File Upload Configuration
# MAX_FILE_SIZE=10485760  # 10MB in bytes
# UPLOAD_PATH=/app/uploads

# Optional: Redis Configuration (for caching, if needed later)
# REDIS_URL=redis://redis:6379/0

# Security Headers
# ALLOWED_HOSTS=103.143.148.190,localhost
# SECURE_SSL_REDIRECT=false  # Set to true if using HTTPS

# Logging
LOG_LEVEL=INFO

# FastAPI Required Environment Variables
DB_USER=postgres
DB_PASS=devpass123
DB_NAME=hub
DB_PORT=5432
JWT_SECRET=dev_secret_key_for_local_development_only
JWT_ALGORITHM=HS256
DB_HOST=db
